<?xml version='1.0' encoding='utf-8'?>
<widget id="com.yingu.player" version="1.2.1" xmlns="http://www.w3.org/ns/widgets" xmlns:cdv="http://cordova.apache.org/ns/1.0" xmlns:android="http://schemas.android.com/apk/res/android">
    <name>音轨</name>
    <description>音轨 - 极简唱片音乐播放器</description>
    <author email="yingu@example.com" href="http://yingu.app">音轨</author>
    <content src="index.html" />

    <!-- 允许访问所有资源 (音频文件) -->
    <access origin="*" />
    <access origin="blob:" />
    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />
    <allow-intent href="tel:*" />
    <allow-intent href="sms:*" />
    <allow-intent href="mailto:*" />
    <allow-intent href="geo:*" />
    <allow-navigation href="*" />

    <!-- 内部导航白名单 -->
    <preference name="AllowInlineMediaPlayback" value="true" />
    <preference name="MediaPlaybackRequiresUserAction" value="false" />
    <preference name="KeepRunning" value="true" />
    <preference name="BackupWebStorage" value="local" />
    <preference name="AutoHideSplashScreen" value="true" />
    <preference name="SplashScreen" value="screen" />
    <preference name="SplashScreenDelay" value="1000" />
    <preference name="FadeSplashScreen" value="true" />

    <!-- Android 配置 -->
    <platform name="android">
        <preference name="android-minSdkVersion" value="24" />
        <preference name="android-targetSdkVersion" value="34" />
        <preference name="android-installLocation" value="auto" />
        <preference name="AndroidLaunchMode" value="singleTop" />
        <preference name="AndroidInsecureFileModeEnabled" value="true" />
        <preference name="MixedContentMode" value="0" />
        <preference name="WebViewBlinkFeature" value="true" />

        <!-- 不在 config.xml 中声明图标，避免文件缺失导致 platform add 失败 -->
        <!-- 图标由插件 Hook 在 before_prepare 阶段自动生成 -->

        <!-- 权限 -->
        <config-file target="app/src/main/AndroidManifest.xml" parent="/manifest">
            <uses-permission android:name="android.permission.INTERNET" />
            <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
            <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
        </config-file>

        <preference name="android-hardwareAccelerated" value="true" />
    </platform>

    <!-- 本地插件通过 package.json 中的 cordova.plugins 配置 -->
</widget>